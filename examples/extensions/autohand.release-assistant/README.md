# Release Assistant

Adds release-range and changelog-context tools plus a release-planning agent.

```sh
autohand extensions validate ./examples/extensions/autohand.release-assistant
autohand extensions install ./examples/extensions/autohand.release-assistant
```

The tools only run when invoked and pass through the normal shell authorization path.

```sh
autohand extensions remove autohand.release-assistant --yes
```
